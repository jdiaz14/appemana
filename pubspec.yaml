name: appemana
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.10
  cupertino_icons: ^1.0.2
  intl: ^0.17.0 # Agregada la dependencia intl

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/product1.jpg
    - assets/product2.jpg

